@model DeffJobTitle

@{
    ViewData["Title"] = "Add Job Title";
}

<div class="page-header">
    <h2><i class="bi bi-plus-circle"></i> Add New Job Title</h2>
</div>

<div class="row">
    <div class="col-md-6 mx-auto">
        <div class="card">
            <div class="card-body p-4">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="mb-3">
                        <label asp-for="JobCode" class="form-label">Job Code <span class="text-danger">*</span></label>
                        <input asp-for="JobCode" class="form-control" placeholder="e.g., MGR" />
                        <span asp-validation-for="JobCode" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="JobName" class="form-label">Job Title <span class="text-danger">*</span></label>
                        <input asp-for="JobName" class="form-control" placeholder="e.g., Sales Manager" />
                        <span asp-validation-for="JobName" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <div class="form-check form-switch">
                            <input asp-for="Active" class="form-check-input" type="checkbox" checked />
                            <label asp-for="Active" class="form-check-label">Active</label>
                        </div>
                    </div>

                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-save"></i> Save
                        </button>
                        <a href="/JobTitles" class="btn btn-secondary">
                            <i class="bi bi-x-circle"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}