@model IEnumerable<Concept.Models.StoreReturnHeader>

@{
    ViewData["Title"] = "إرجاع المخزون";
    var filter = ViewBag.Filter ?? "all";
}

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col-md-6">
            <h2>إرجاع المخزون</h2>
        </div>
        <div class="col-md-6 text-end">
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> إرجاع جديد
            </a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Filter Tabs -->
    <ul class="nav nav-tabs mb-3">
        <li class="nav-item">
            <a class="nav-link @(filter == "all" ? "active" : "")" asp-action="Index" asp-route-filter="all">
                الكل <span class="badge bg-secondary">@ViewBag.AllCount</span>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(filter == "pending" ? "active" : "")" asp-action="Index" asp-route-filter="pending">
                قيد الانتظار <span class="badge bg-warning text-dark">@ViewBag.PendingCount</span>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(filter == "approved" ? "active" : "")" asp-action="Index" asp-route-filter="approved">
                تمت الموافقة <span class="badge bg-success">@ViewBag.ApprovedCount</span>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(filter == "rejected" ? "active" : "")" asp-action="Index" asp-route-filter="rejected">
                مرفوض <span class="badge bg-danger">@ViewBag.RejectedCount</span>
            </a>
        </li>
    </ul>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>رقم الإرجاع</th>
                            <th>كود الإرجاع</th>
                            <th>نوع الإرجاع</th>
                            <th>التاريخ</th>
                            <th>من</th>
                            <th>إلى</th>
                            <th>الحالة</th>
                            <th>المستخدم</th>
                            <th>الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Any())
                        {
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.ReturnNo</td>
                                    <td>@item.ReturnCode</td>
                                    <td>
                                        @switch (item.ReturnType)
                                        {
                                            case 1:
                                                <span class="badge bg-info">مخزن إلى مخزن</span>
                                                break;
                                            case 2:
                                                <span class="badge bg-primary">مخزن إلى قسم</span>
                                                break;
                                            case 3:
                                                <span class="badge bg-warning text-dark">قسم إلى مخزن</span>
                                                break;
                                            case 4:
                                                <span class="badge bg-secondary">قسم إلى قسم</span>
                                                break;
                                            default:
                                                <span class="badge bg-light text-dark">غير معروف</span>
                                                break;
                                        }
                                    </td>
                                    <td>@item.ReturnDate.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        @if (item.ReturnType == 1 || item.ReturnType == 2)
                                        {
                                            <span>مخزن (@item.FromWarehouseId)</span>
                                        }
                                        else
                                        {
                                            <span>قسم (@item.FromDepartmentId)</span>
                                        }
                                    </td>
                                    <td>
                                        @if (item.ReturnType == 1 || item.ReturnType == 3)
                                        {
                                            <span>مخزن (@item.ToWarehouseId)</span>
                                        }
                                        else
                                        {
                                            <span>قسم (@item.ToDepartmentId)</span>
                                        }
                                    </td>
                                    <td>
                                        @switch (item.ReturnStatus)
                                        {
                                            case 0:
                                                <span class="badge bg-warning text-dark">قيد الانتظار</span>
                                                break;
                                            case 1:
                                                <span class="badge bg-success">تمت الموافقة</span>
                                                break;
                                            case 2:
                                                <span class="badge bg-danger">مرفوض</span>
                                                break;
                                            default:
                                                <span class="badge bg-secondary">غير معروف</span>
                                                break;
                                        }
                                    </td>
                                    <td>@(item.User?.FullName ?? "N/A")</td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info" title="التفاصيل">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            @if (item.ReturnStatus == 0)
                                            {
                                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning" title="تعديل">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                            }
                                            @if (item.ReturnStatus != 1)
                                            {
                                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline" onsubmit="return confirm('هل أنت متأكد من حذف هذا الإرجاع؟');">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-sm btn-danger" title="حذف">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </form>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="9" class="text-center">لا توجد عمليات إرجاع</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Auto-hide alerts after 5 seconds
        setTimeout(function () {
            $('.alert').fadeOut('slow');
        }, 5000);
    </script>
}
